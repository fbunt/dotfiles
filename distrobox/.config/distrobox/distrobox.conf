# distrobox global config
# Enter boxes in the container's own home dir, not the host cwd (restores
# pre-1.8 behavior; otherwise enter drops you in /run/host/<host-cwd>).
skip_workdir=1

# New boxes get an isolated per-box home at <prefix>/<box-name>.
container_home_prefix="/var/home/fred/homes"
